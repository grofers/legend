apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "legend.fullname" . }}-test-connection"
  labels:
    {{- include "legend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "legend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
